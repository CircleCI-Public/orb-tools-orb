version: 2.1

orbs:
  cli: circleci/circleci-cli@volatile

jobs:
  build:
    executor: cli/default
    steps:
      - run: mkdir .circleci
      - run: 'echo "nope: not valid" > orb.yml'
      - orb-tools/validate:
          orb-path: orb.yml

workflows:
  test-orb-validate:
    jobs:
      - build
